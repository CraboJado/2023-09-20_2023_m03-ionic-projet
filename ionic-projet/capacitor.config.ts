import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-projet',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
